-- ::= import "core.ic"
cstdio ::= import "cstdio.ic"
io ::= import "io.ic"

PartOne ::= (nums: $nums) -> i64 {
  n := (@nums):?.length as i64

  i := 0
  while (i < n) do {
    target := 2020 - (@nums)[i]
    j := i + 1
    while (j < n) do {
      if (target == (@nums)[j]) then {
        return (@nums)[i] * (@nums)[j]
      }
      j += 1
    }
    i += 1
  }
}


test := [1721, 979, 366, 299, 675, 1456]
io.Print("Part 1 (test): ")
io.Print(PartOne(&test))
io.Print("\n")

